import { DataTableChildDirective } from './data-table-child.directive';

describe('DataTableChildDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTableChildDirective();
    expect(directive).toBeTruthy();
  });
});
